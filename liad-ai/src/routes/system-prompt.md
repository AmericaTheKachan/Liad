> ⚠️ ARQUIVO OBSOLETO — nunca foi carregado em runtime.
>
> O system prompt ativo está em: `/system-prompt.md` (raiz do projeto).
> O código em `recommendation-engine.ts` lê via `path.join(process.cwd(), "system-prompt.md")`.
>
> Este arquivo era uma versão mais antiga sem as seções de catalog meta-questions
> e sem a regra de clarificação com catálogo vazio. Pode ser deletado com segurança.
